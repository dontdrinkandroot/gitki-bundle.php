imports:
    - { resource: ../config.yml }

framework:
    router:
        resource: "%kernel.project_dir%/Tests/Utils/Application/app/config/elasticsearch/routing.yaml"

ddr_gitki:
    elasticsearch:
        index_name: gitki_test
