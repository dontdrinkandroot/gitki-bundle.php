imports:
    - { resource: ../config.yml }

framework:
    router:
        resource: "%kernel.root_dir%/config/elasticsearch/routing.yml"
    templating: false

ddr_gitki:
    elasticsearch:
        index_name: gitki_test
