{% extends 'App::layout.html.twig' %}

{% set title = 'Отправить на потребителско Имя' %}
{% block content %}
<p>Е, на всекиго може да се случи да си забрави Имято. ;-) Няма страшно! Ако в потребителските си данни сте посочили валидна електронна поща, сега можете да поискате напомняне за Имято, с което сте се регистрирали в <em>Моята библиотека</em>.</p>

{{ form_start(form, { attr: {class: "form-horizontal"} }) }}
	{{ form_errors(form) }}

	<div class="form-group">
		{{ form_label(form.email, 'Електронна поща', { label_attr: {class: 'col-sm-2 control-label'} }) }}
		<div class="col-sm-10">
			{{ form_errors(form.email) }}
			{{ form_widget(form.email, { attr: {class: 'form-control'} }) }}
		</div>
	</div>
	<div class="form-submit">
		{{ form_widget(form.save, { attr: {class: 'btn btn-primary'} , label: 'Отправить на потребителското Имя' }) }}
	</div>
{{ form_end(form) }}
{% endblock content %}
