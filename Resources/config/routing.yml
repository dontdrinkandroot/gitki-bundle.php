ddr_gitki_search:
    path:   /search/
    defaults: { _controller: DdrGitkiBaseBundle:Search:search }

ddr_gitki_history:
    path:   /history
    defaults: { _controller: DdrGitkiBaseBundle:Repository:history }

ddr_gitki_directory:
    path:   /browse{path}
    defaults: { _controller: DdrGitkiBaseBundle:Repository:directory }
    requirements:
        path: .*/

ddr_gitki_file:
    path:   /browse{path}
    defaults: { _controller: DdrGitkiBaseBundle:Repository:file }
    requirements:
        path: .+
